/*document.addEventListener('DOMContentLoaded', function () {
    var seccionesContainer = document.getElementById('seccionesContainer');

    seccionesContainer.addEventListener('click', function (event) {
        var target = event.target;
        // Verifica si el clic se realizó en una tarjeta
        if (target.classList.contains('card')) {
            // Obtiene el contenido de la tarjeta clicada
            var contenidoTarjeta = target.innerHTML;
            // Actualiza el contenido de la sección
            seccionesContainer.innerHTML = contenidoTarjeta;
        }
    });
});*/
